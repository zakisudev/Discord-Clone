import MainPageButton from '../../components/MainPageButton';

const SideBar = () => {
  return (
    <div className="w-[72px] h-screen flex flex-col items-center bg-gray-800">
      <MainPageButton />
    </div>
  );
};

export default SideBar;
